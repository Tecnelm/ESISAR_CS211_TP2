#include <stdio.h>
#include <stdlib.h>
void get_nombre_impairs(void);
int *get_info_char(const char *folder_name);
void is_inside_test();
int is_inside(char c,char *table,int table_size);
int is_upper(char c);
int is_lower(char c);
void aff_value(int *table,const char* folder_name);